import DashboardClient from "@/components/app/DashboardClient";

// Dashboard is fully client-side for real-time data + loading states
export default function DashboardPage() {
  return <DashboardClient />;
}
